# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aks-multiregion-internal
metadata:
  template: aks-multiregion-internal@0.0.1-beta

infra:
  provider: bicep
  path: infra
  module: main

hooks:
  postprovision:
    windows:
      shell: pwsh
      run: ./scripts/sync-gitops-config.ps1
