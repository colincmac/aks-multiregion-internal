apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: azure-alb-controller
  namespace: azure-alb-system
spec:
  values:
    albController:
      podIdentity:
        clientId: PLACEHOLDER_WEST_ALB_CONTROLLER_CLIENT_ID
