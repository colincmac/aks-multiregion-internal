apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Optional demo overlay. Not referenced from `clusters/base/my-app`; opt in
# from a per-cluster overlay by adding `../base/my-app/demo-traffic` as a
# resource.
resources:
  - virtual-service-retries-canary.yaml
  - virtual-service-fault-injection.yaml
  - destination-rule-sticky.yaml
  - cross-region-weighted.yaml
