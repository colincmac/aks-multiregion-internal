apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Tier-1 gateway overlay — Application Gateway for Containers (AGC).
# Implements ADR-0001 Option E. Not yet included from any regional
# kustomization; see ./README.md for wiring steps.
resources:
  - namespace.yaml
  - applicationloadbalancer.yaml
  - gateway.yaml
  - httproute.yaml
  - backendtlspolicy.yaml
