apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: external-dns
  labels:
    app.kubernetes.io/name: external-dns
  # Workload Identity annotation is added via per-cluster Kustomize patch
  # once the Azure Managed Identity client ID is known.
  # annotations:
  #   azure.workload.identity/client-id: <MANAGED_IDENTITY_CLIENT_ID>
