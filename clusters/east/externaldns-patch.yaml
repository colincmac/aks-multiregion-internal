# Per-cluster ExternalDNS patch for the East cluster.
# Overrides --txt-owner-id so this ExternalDNS instance only manages the
# east cluster's A record contribution, and sets the correct Azure subscription
# and resource group for the Private DNS Zone.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-dns
  namespace: external-dns
spec:
  template:
    spec:
      containers:
        - name: external-dns
          args:
            - --source=service
            - --provider=azure-private-dns
            - --azure-resource-group=PLACEHOLDER_GLOBAL_RESOURCE_GROUP
            - --azure-subscription-id=PLACEHOLDER_AZURE_SUBSCRIPTION_ID
            - --domain-filter=PLACEHOLDER_PRIVATE_DNS_ZONE_NAME
            - --policy=sync
            - --registry=txt
            - --txt-owner-id=cluster-east
            - --txt-prefix=externaldns-
            - --interval=10s
            - --log-level=info
            - --log-format=json
